# thymeleaf
spring.thymeleaf.cache=false
# spring mvc
spring.mvc.view.prefix=classpath:/templates/
spring.mvc.view.suffix=.html
# db
spring.datasource.url=jdbc:h2:file:D:/db/car_reserve
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
# indicate the db platform is h2
spring.datasource.platform=h2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# h2 console
# in order to visit it via localhost:8080/h2
spring.h2.console.path=/h2
# in order to visit it remotely, otherwise, it could just be visited in local
spring.h2.console.settings.web-allow-others=true
spring.h2.console.enabled=true
# data init
# after configured, every time started, program will run resources/db/schema.sql to operate the db structure
#spring.datasource.schema=classpath:db/schema.sql
# after configured, every time started, program will run resources/db/data.sql to operate the data
#spring.datasource.data=classpath:db/data.sql

